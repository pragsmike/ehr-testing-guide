<!--
Copy this block for every entry in Part IV (40-part-4-reference/). Never
write a Part IV entry freeform — the value of Part IV is that every entry
answers the same questions in the same order. Delete rows that genuinely
don't apply (e.g. "Interop mode" for a terminology rather than a tool)
rather than leaving them blank.
-->

### <Name>

| Field | Value |
|---|---|
| What it is | |
| Publisher / steward | |
| Kind of object (in this book's terms) | delta / state / attested snapshot / terminology / profile / tool |
| Interop mode (tools only) | native Clojure / Java interop / subprocess / HTTP |
| Maintenance status (tools only) | active / stable / dormant / abandoned |
| Role in a test plan | |
| Where it bites | |
| Entry last verified | YYYY-MM-DD |

<!-- The "Kind of object" list above is a starting vocabulary, not a closed
     enum. When a standard is genuinely none of these, name its kind
     honestly rather than force-fitting it into the nearest listed option —
     see Chapter 41's entries for v3/RIM (a modeling framework), IHE (a
     workflow composition), SMART on FHIR (access machinery), and CQL (an
     observation language). -->
